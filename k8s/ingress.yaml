apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: krona-backend
  namespace: krona
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  rules:
    - host: krona-api.thesis-i.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: krona-backend
                port:
                  number: 8000
